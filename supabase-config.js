// Fill these in once your Supabase project exists (Settings → API).
// The anon key is safe to expose client-side — it can only do what the
// RLS policies in supabase/schema.sql allow (anonymous inserts only).
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
